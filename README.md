# ShakeDrop-Replication
ShakeDrop-Replication implements a ResNet-like architecture with ShakeDrop regularization, which randomly perturbs residual block outputs to improve generalization. It includes customizable residual blocks, shortcut layers, pooling, and fully connected layers for end-to-end training.
