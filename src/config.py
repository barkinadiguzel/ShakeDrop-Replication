# Hyperparameters
channels = [16, 32, 64]
layers = [2, 2, 2]  # Residual block counts per stage
p_drops = [0.5, 0.5, 0.5]  # ShakeDrop drop probabilities
alpha_range = (-1, 1)
beta_range = (0, 1)
num_classes = 10
